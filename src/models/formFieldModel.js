const FormFieldModel = ({
    id,
    name,
    label,
    type,
    options,
    row,
    col, 
    width,
}) => {
    this.id = id;
    this.name = name;
    this.label = label;
    this.type = type;
    this.options = options;
    this.row = row;
    this.col = col;
    this.width = width;
}

